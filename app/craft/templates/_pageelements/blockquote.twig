{#
 # Template for 'blockQuote' Page Element:
 #}
<blockquote>
  {{ element.blockContent }}
  {% if element.reference is not empty %}
  <cite>{{ element.reference }}</cite>
  {% endif %}
</blockquote>
